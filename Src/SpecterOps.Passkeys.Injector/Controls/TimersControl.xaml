<UserControl x:Class="SpecterOps.Passkeys.Injector.TimersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="64" d:DesignWidth="360">
    <UserControl.Resources>
        <Style x:Key="CountdownLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,6,12,6"/>
        </Style>
        <Style x:Key="CountdownValueStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,6,0,6"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
        <TextBlock Text="Request Expiration:" Style="{StaticResource CountdownLabelStyle}"/>
        <TextBlock x:Name="RequestCountdownText" Style="{StaticResource CountdownValueStyle}" Text="--:--"/>
        <TextBlock Text="Challenge JWT Expiration:" Style="{StaticResource CountdownLabelStyle}" Margin="24,6,12,6"/>
        <TextBlock x:Name="ChallengeCountdownText" Style="{StaticResource CountdownValueStyle}" Text="N/A"/>
    </StackPanel>
</UserControl>
